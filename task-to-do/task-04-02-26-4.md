# Task 4 - HAUTE : Gater le debug panel derriere import.meta.env.DEV

## Fichier a modifier
- `src/components/settings/ProfileSettings.jsx` (lignes 499-570+)

## Probleme
Le Debug Panel Supabase (Accordion avec logs en temps reel, test de connexion, etc.) est visible en production.

## Solution
Entourer le bloc `{/* Debug Panel Accordion */}` d'une condition `import.meta.env.DEV`.

## Code attendu
```jsx
{import.meta.env.DEV && (
  <Accordion type="single" collapsible className="w-full bg-gray-900 border border-gray-800 rounded-lg">
    {/* ... tout le debug panel existant ... */}
  </Accordion>
)}
```

## Statut
- [ ] Complete
