# Task 022 - [FAIBLE] : Integration dans AccountingIntegration

## Fichier(s) a modifier
- `src/pages/AccountingIntegration.jsx`

## Probleme
Le composant BalanceSheetInitializer doit etre accessible depuis l'interface Comptabilite.

## Solution
Ajouter un nouvel onglet "Initialisation" dans AccountingIntegration.jsx

## Implementation
1. Importer BalanceSheetInitializer
2. Ajouter un nouvel onglet dans la liste des tabs
3. Afficher le composant quand l'onglet est selectionne

## Code a ajouter

### Import (en haut du fichier)
```javascript
import BalanceSheetInitializer from '@/components/accounting/BalanceSheetInitializer';
```

### Dans la liste des tabs (chercher le tableau tabs ou equivalent)
Ajouter apres les tabs existants:
```javascript
{ id: 'init', label: 'Initialisation', icon: Settings2 }
```

### Dans le rendu conditionnel des tabs
Ajouter le case pour 'init':
```javascript
{activeTab === 'init' && (
  <BalanceSheetInitializer onComplete={() => refresh()} />
)}
```

## Criteres de verification
- [ ] Import ajoute
- [ ] Nouvel onglet visible dans l'interface
- [ ] Composant BalanceSheetInitializer s'affiche correctement
- [ ] Build passe

## Statut
- [ ] Complete
