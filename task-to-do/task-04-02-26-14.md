# Task 14 - MOYENNE : Hook useInvoiceExtraction

## Fichiers a creer
- `src/hooks/useInvoiceExtraction.js` (CREER)

## Probleme
Aucun hook React n'existe pour gerer l'etat d'extraction IA des factures.

## Solution
Creer un hook qui wrape le service extractInvoiceData avec gestion d'etat React (extracting, extractedData, extractionError).

## Code attendu

```javascript
import { useState } from 'react';
import { extractInvoiceData } from '@/services/invoiceExtractionService';
import { useToast } from '@/components/ui/use-toast';

export const useInvoiceExtraction = () => {
  const [extracting, setExtracting] = useState(false);
  const [extractedData, setExtractedData] = useState(null);
  const [extractionError, setExtractionError] = useState(null);
  const { toast } = useToast();

  const extractInvoice = async (filePath, fileType, userId) => {
    setExtracting(true);
    setExtractionError(null);

    try {
      const data = await extractInvoiceData({ filePath, fileType, userId });
      setExtractedData(data);
      toast({
        title: 'Extraction complete',
        description: 'Invoice data has been extracted. Please review and confirm.',
      });
      return data;
    } catch (err) {
      const message = err.message === 'insufficient_credits'
        ? 'Not enough credits for AI extraction'
        : err.message === 'extraction_failed'
        ? 'Could not extract data from this document. Please fill in manually.'
        : err.message;

      setExtractionError(message);
      toast({
        title: 'Extraction failed',
        description: message,
        variant: 'destructive',
      });
      return null;
    } finally {
      setExtracting(false);
    }
  };

  const clearExtraction = () => {
    setExtractedData(null);
    setExtractionError(null);
  };

  return {
    extractInvoice,
    extracting,
    extractedData,
    extractionError,
    clearExtraction,
  };
};
```

## Criteres de verification
- [ ] Fichier `src/hooks/useInvoiceExtraction.js` existe
- [ ] Import extractInvoiceData depuis le service
- [ ] Etats : extracting, extractedData, extractionError
- [ ] Fonction extractInvoice avec try/catch et toast
- [ ] Fonction clearExtraction
- [ ] Gestion erreur insufficient_credits et extraction_failed

## Statut
- [ ] Complete
